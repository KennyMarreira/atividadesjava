package application;

import java.util.Locale;
import java.util.Scanner;

import entities.Quarto;

public class Program {

	public static void main(String[] args) {
		
		Locale.setDefault(Locale.US);
		Scanner sc = new Scanner(System.in);
		Quarto vect[] = new Quarto[10];
		
		System.out.println("Digite a quantidade de quartos alugados: ");
		int n = sc.nextInt();
		
		for (int i = 0; i < n; i++) {
			System.out.printf("Aluguel %d#:%n", i+1);
			System.out.printf("Nome: ");
			String nome = sc.next();
			sc.nextLine();
			System.out.printf("%nE-mail: ");
			String email = sc.next();
			sc.nextLine();
			System.out.printf("%nQuarto: ");
			int quarto = sc.nextInt();
			vect[quarto] = new Quarto(nome, email, quarto);
		}
		
		System.out.println("Quartos alugados:");
		for (int i = 0; i < 10; i++) {
			if (vect[i] != null) {
				System.out.println(vect[i].toString());
			}
		}
		
		
		sc.close();
	}

}
